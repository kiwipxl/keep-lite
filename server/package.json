{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "set PGUSER=postgres&& set PGHOST=localhost&& set PGDATABASE=postgres&& set PGPORT=5432&& node index.js",
    "test": "set PGUSER=postgres&& set PGHOST=localhost&& set PGDATABASE=testing&& set PGPORT=5432&& jest --watch --runInBand"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@drdgvhbh/postgres-error-codes": "0.0.6",
    "jest": "^26.6.3",
    "pg": "^8.5.1"
  },
  "devDependencies": {
    "prettier": "2.2.1"
  },
  "jest": {
    "modulePathIgnorePatterns": [
      "src",
      "__tests__/src"
    ]
  }
}
